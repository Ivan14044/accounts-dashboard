# CLAUDE.md — правила работы с этим проектом

Файл читается ассистентом в начале каждой сессии. Он описывает **как работать**
(режим, цикл задачи, самопроверка) и фиксирует **проверенные факты** о проекте.

Порядок приоритетов при конфликте: (1) прямое указание владельца в чате →
(2) этот файл → (3) всё остальное (комментарии, README, openspec, привычки).
`AGENTS.md` — управляемый блок openspec, его правила про спеки не отменяют этот файл.

---

## ⛓️‍💥 Главное правило: проверяй РЕАЛЬНОСТЬ, а не комментарии и доки

Комментарии в коде, README, doc-файлы и openspec **могут врать или устаревать**.
Никогда не делай выводы и не давай рекомендаций на основе одного лишь текста рядом
с кодом. Проверяй, как система ведёт себя **на самом деле**.

Конкретно, прежде чем что-то утверждать или менять:

1. **Читай реальный код по цепочке**, а не его описание. Если комментарий говорит
   «лимит 100» или «иначе nginx 429» — найди, где это реально задаётся и кем
   накладывается (наш nginx? внешний API? PHP-таймаут?).
2. **Воспроизводи поведение вживую.** Внешний API — дёрни его настоящим `curl`
   (с заведомо тестовыми/фейковыми данными) и посмотри фактический формат ответа,
   коды, тайминги, реальные лимиты. Фронт/таблицу — подними локальный стенд
   (см. «Локальный стенд») и **измерь** в браузере, а не догадывайся: реальный
   замер `scrollWidth/clientWidth`, а не «по комментарию должно скроллиться».
3. **Пиши тесты / проверочные скрипты** на ключевую логику и краевые случаи.
   Особенно на то, что портит данные (классификация valid/invalid, дедуп,
   импорт/экспорт). Тест должен падать на старом баге и проходить на фиксе.
4. **Числа из чужих слов и из комментариев — гипотеза, а не факт.** Подтверждай
   измерением. Пример: заявленный «лимит 3000 за запрос» у check.fb.tools при
   проверке оказался не потолком — API принимает и 5000 (HTTP 200, ~8с); реальное
   ограничение — время/размер ответа, а не число 3000.
5. **Если проверить невозможно** (нет доступа, нет окружения) — скажи об этом явно
   и не выдавай предположение за установленный факт.

Цель: выводы и правки опираются на наблюдаемое поведение, а не на то, что
кто-то когда-то написал в комментарии.

---

## Режим работы: автономный, качество важнее скорости

Владелец не хочет пошагового согласования. Работай самостоятельно и доводи задачу
до конца — включая тесты, живую проверку и документацию. Объём работы не повод
сокращать: если надо перелопатить 30 файлов и поднять стенд — делай.

### Решай сам (не спрашивай)
- Имена переменных/функций/файлов, структура модуля, порядок аргументов.
- Локальные рефакторинги внутри задачи, вынос дублей, разбиение длинной функции.
- Выбор способа проверки: тест / curl / стенд / замер в браузере.
- Мелкие UI-детали в рамках существующего дизайна: отступы, состояния
  hover/focus/disabled, тексты подсказок, порядок пунктов меню.
- Исправление явного бага, найденного по пути, если фикс маленький и покрыт тестом
  (обязательно расскажи об этом в отчёте отдельным пунктом).
- Добавление комментария/докблока, обновление документации.

Правило принятия решения: сформулируй 2–3 варианта, оцени их по критериям
«корректность данных → простота → согласованность с текущим кодом →
производительность», выбери и **зафиксируй причину выбора** в комментарии или в
отчёте. Не устраивай опрос владельца по вопросам, которые решаются этим анализом.

### Спрашивай обязательно (стоп-лист)
- Любая правка/удаление данных в боевой БД, ручные миграции на проде.
- Смена контракта API, формата экспорта/импорта, схемы БД, семантики статусов.
- Удаление фич, файлов или таблиц; массовые переименования публичных функций.
- Новые внешние зависимости и новые обращения к сторонним сервисам.
- Всё, что меняет поведение денег/доступов/безопасности.

### Мёрдж и деплой — делаю сам

Владелец выдал постоянное разрешение: **мержить PR в `main` можно без отдельного
спроса**, хотя мёрдж = немедленный деплой в прод. Разрешение действует, пока
владелец не скажет обратное («не мержи», «этот PR не трогай»).

Мержу, только когда одновременно выполнено:
- чек `lint` зелёный (не «должен пройти», а увидел зелёным);
- цикл задачи пройден целиком: тесты на 7.3 и 8.2, самопроверка, живая проверка;
- в изменениях нет ничего из стоп-листа выше — а если было, согласие получено
  **до** реализации, а не перед кнопкой мёрджа.

После мёрджа доводи до конца, а не отчитывайся по факту нажатия кнопки:
1. Дождись workflow **Deploy to FTP** и убедись, что он `success`.
2. Открой прод и проверь, что страница живая (HTTP 200, ключевой сценарий цел).
3. Если деплой красный или прод сломан — **сразу** скажи владельцу и откатывай
   (`gh pr revert` / revert-коммит + новый PR), не пытайся чинить прод вслепую.
4. В отчёте: что смержено, статус деплоя, чем подтверждена живость прода.

Если сомневаешься, готово ли — не мержи. Разрешение снимает необходимость
спрашивать, но не отменяет ответственность за то, что уехало на живой сайт.

### Улучшения «от себя»
Бойскаутское правило в границах: улучшай то, чего касаешься. Заметил проблему
вне задачи — не расширяй молча PR: либо мелкий безопасный фикс с тестом и
пометкой в отчёте, либо запись в отчёт «нашёл, не трогал, вот почему».
Не превращай задачу в бесконечный рефакторинг всего проекта.

---

## Обязательный цикл задачи

1. **Разведка.** Прочитай реальный код по цепочке фронт → API → сервис → БД.
   Найди, где логика уже реализована, чтобы не плодить дубль (в проекте это
   реальная боль: см. предупреждение в шапке `dashboard-stats.js`).
2. **План.** Сформулируй, что меняется, какие краевые случаи, что может сломаться,
   как это будет проверено. Для крупных изменений — сначала openspec-предложение
   (см. `openspec/AGENTS.md`).
3. **Тест вперёд.** Сначала тест, который **падает** на текущем поведении.
   Убедись глазами, что он красный и падает по правильной причине.
4. **Реализация** минимальным понятным кодом в конвенциях проекта.
5. **Тест зелёный** — прогони весь набор, а не один файл, и на обеих версиях PHP.
6. **Самопроверка** по чеклисту ниже (обязательно, каждый раз).
7. **Живая проверка** на локальном стенде: страница рендерится, консоль чистая,
   `php_errors.log` без новых Warning/Fatal, UI-сценарий отрабатывает.
8. **Документация и комментарии** обновлены (см. раздел про доки).
9. **Отчёт владельцу:** что сделано, чем это подтверждено (команды и результат),
   какие решения приняты самостоятельно и почему, что осталось за скобками.

---

## Самопроверка: после каждой функции и каждого модуля

Не «написал всё → проверил в конце». Проверяй по мере: закончил осмысленный
кусок — сразу прогони его.

**После каждой функции:**
- Что она возвращает на пустом входе, `null`, `0`, `''`, на огромном входе?
- Все ветки кода реально достижимы? Есть ли ранний выход, который всё глотает?
- Ошибка внешнего мира (БД, сеть, API) отличима от «данные плохие»? Сбой **никогда**
  не должен молча превращаться в бизнес-результат (см. правило valid/invalid/errored).
- Побочные эффекты названы в имени функции? Функция `getX()` не должна писать в БД.
- Есть докблок: назначение, параметры, возврат, неочевидные краевые случаи.

**После каждого модуля / перед тем, как считать задачу сделанной:**
- `php -l` по изменённым файлам + весь набор тестов на PHP 7.3 **и** 8.2
  (7.3 — нижняя граница, потому что прод старее 7.4).
- Перечитай собственный diff целиком, как чужой код: нет ли отладочного мусора,
  закомментированного кода, `console.log`, TODO без объяснения, лишних файлов.
- Не сломаны ли соседние сценарии: экспорт, импорт, корзина, undo, фильтры,
  избранное — если менял общие места (`AccountsService`, `FilterBuilder`,
  партиалы таблицы, `Database`).
- Совпадает ли поведение с тем, что написано в комментариях и доках рядом.
  Если нет — правится **и то, и другое**.
- Живая проверка выполнена и её результат ты видел своими глазами.

Формулируй результат честно: «проверено так-то, вот вывод команды». Если что-то
не проверено — так и пиши, а не «должно работать».

---

## Тесты

Тесты лежат в `tests/`, фикстуры — в `tests/fixtures/`. Локального PHP на машине
нет, всё гоняется в Docker.

```bash
docker run --rm -v "$PWD":/app -w /app php:7.3-cli bash -c 'for t in tests/test_*.php; do echo "── $t"; php "$t" || exit 1; done'
```

Правила:
- **Имя `tests/test_*.php`** — такой файл автоматически попадает в CI-гейт
  (`lint.yml` гоняет их глобом). Тест обязан быть самодостаточным: без сети,
  без БД, без секретов; код выхода 0 = успех, 1 = падение.
- Тесты, требующие БД, называются иначе (`tests/integration_*.php`) и **сами
  скипаются** (exit 0), если нет `TEST_DB_*` — рецепт запуска описан в шапке
  `tests/integration_undo_mysql.php`.
- Проверяй на **обеих границах**: `php:7.3-cli` (нижняя — прод старее 7.4, см.
  «Проверенные факты») и `php:8.2-cli` (верхняя). Разница реальна в обе стороны:
  `count()` без аргумента на 7.x — Warning, на 8.x — фатальная
  `ArgumentCountError`; стрелочная функция `fn() =>` работает с 7.4, а на проде
  даёт Parse error.
- **Не используй синтаксис новее PHP 7.3** в коде, который уезжает на прод:
  никаких `fn() =>`, `??=`, типизированных свойств, `[$a, $b] = ...`. То же
  относится к тестам — иначе гейт врёт о совместимости.
- Тест на баг пишется **до** фикса и должен падать. Красный тест, который ты не
  видел красным, ничего не доказывает.
- Приоритет покрытия: всё, что может **испортить данные** — классификация статусов,
  undo, дедуп, импорт/экспорт CSV, валидация ID, миграции; и вся чистая логика,
  которую можно вызвать без БД.
- В тесте включай строгий режим: `set_error_handler`, превращающий Warning/Notice
  в исключение (пример — `tests/test_empty_state_colspan.php`). На проде
  `error_reporting=E_ALL`, и Warning в шаблоне реально обрывает рендер.
- **`"«$var»"` — не то, что кажется.** В именах переменных PHP разрешены байты
  `\x80-\xff`, поэтому «ёлочки» и любые другие не-ASCII символы вплотную к
  имени приклеиваются к нему: `«$param»` разбирается как переменная
  `param\xC2\xBB`, а не как `$param` в кавычках. Получаем `Undefined variable`
  на ровном месте. Пиши `«{$param}»`. *(наступили 2026-08-23 в
  `tests/test_presence_filters.php`.)*

---

## Локальный стенд

*(Рецепт поднят и проверен целиком 2026-08-08; имена и порт сделаны уникальными
и перепроверены 2026-08-10 — стенд поднят ОДНОВРЕМЕННО с чужим, оба работали.)*

Полноценный дашборд поднимается локально за пару минут — используй его для живой
проверки вместо гаданий и вместо экспериментов на проде.

**Имена контейнеров и порт обязаны быть уникальными на воркспейс.** Раньше рецепт
использовал фиксированные `dash-php`/`dash-mysql`/`dash-local` и порт 8099, и две
параллельные сессии дрались за них: вторая либо не поднималась, либо перетирала
стенд первой. Так и случилось 2026-08-10 — соседняя сессия пересоздала контейнеры
с теми же именами. Имена теперь берутся из имени каталога воркспейса, а порт
выдаёт сам Docker (`-p 0:8080`), поэтому свободный он всегда.

```bash
DASH_ID=$(basename "$PWD")
DASH_NET="dash-net-$DASH_ID"; DASH_DB="dash-mysql-$DASH_ID"; DASH_PHP="dash-php-$DASH_ID"

docker network create "$DASH_NET"
docker run -d --name "$DASH_DB" --network "$DASH_NET" -e MYSQL_ALLOW_EMPTY_PASSWORD=yes -e MYSQL_DATABASE=dashboard mysql:8.0
docker run -d --name "$DASH_PHP" --network "$DASH_NET" -p 0:8080 -v "$PWD":/app -w /app php:7.4-cli sh -c 'docker-php-ext-install mysqli >/dev/null 2>&1; PHP_CLI_SERVER_WORKERS=4 php -S 0.0.0.0:8080'

# Порт спрашиваем у Docker. Переменные оболочки между вызовами не живут —
# в каждой новой оболочке пересчитывай эти три строки, они идемпотентны.
DASH_PORT=$(docker port "$DASH_PHP" 8080 | head -1 | sed 's/.*://')
echo "стенд: http://localhost:$DASH_PORT/login.php"
```

Важные детали, подтверждённые на практике:
- **Никогда не удаляй контейнеры `dash-*`, чьё имя не совпадает с твоим
  `$DASH_ID`.** Это чужой стенд из параллельной сессии; проверить можно так:
  `docker inspect <имя> --format '{{range .Mounts}}{{.Source}}{{end}}'` — там
  будет путь к другому воркспейсу.
- **Схему создавать руками не нужно.** `sql/create_all_tables.sql` падает на пустой
  базе (`account_history` ссылается на ещё не существующую `accounts`). Таблицу
  `accounts` и индексы создаёт само приложение при первом заходе —
  `includes/DatabaseSchemaManager.php::validateAndMigrate()`.
- Вход — только строка подключения (пароль пользователя не нужен, БД без пароля).
  Хост — это имя КОНТЕЙНЕРА с базой, то есть `$DASH_DB`:
  `server=$DASH_DB;port=3306;user id=root;password=;database=dashboard`.
- Логин скриптом (без формы) — забрать CSRF и отправить POST:

```bash
TOKEN=$(curl -s -c cj.txt "http://localhost:$DASH_PORT/login.php" | grep -o 'name="csrf_token" value="[^"]*"' | sed 's/.*value="//;s/"//'); curl -s -b cj.txt -c cj.txt -o /dev/null -w "%{http_code} %{redirect_url}\n" -d "csrf_token=$TOKEN" -d "db_connection_string=server=$DASH_DB;port=3306;user id=root;password=;database=dashboard" "http://localhost:$DASH_PORT/login.php"
```

- Тестовые строки наливай сам (`INSERT INTO accounts ...`) — только заведомо
  фейковые данные, никаких боевых дампов на локалку.
- Ошибки PHP пишутся в `php_errors.log` в корне (в `.gitignore`). Перед проверкой
  чисти его (`: > php_errors.log`) и после сценария смотри, что он пуст.
- Убрать за собой (только своё): `docker rm -f "$DASH_PHP" "$DASH_DB" && docker network rm "$DASH_NET"`.

---

## Живая проверка в браузере

Правило: **UI считается работающим только после того, как ты открыл его в браузере
и измерил.** Скриншот показывает, что нарисовалось; JS-замеры показывают правду о
вёрстке; консоль показывает, что сломалось молча.

Чем пользоваться:
- **Локальный стенд + встроенный браузер** (`mcp__Claude_Browser__*`) — основной
  способ. Своя сессия, можно жать любые кнопки, ничего не испортишь.
- **Chrome владельца** (`mcp__claude-in-chrome__*`) — только когда нужна его живая
  сессия на проде. Там: **read-only**. Смотреть, читать консоль/сеть, мерить.
  Ничего не удалять, не сохранять, не запускать массовые операции, не вводить
  пароли и токены. Любое действие, меняющее данные на проде, — сначала спросить.

Проверенный порядок работы со встроенным браузером:
1. `preview_start` с `url: http://localhost:$DASH_PORT/login.php` — порт свой у
   каждого воркспейса, узнаётся через `docker port "$DASH_PHP" 8080`
   (см. «Локальный стенд»).
2. Размер вьюпорта задавать **числами**: `resize_window {width: 1280, height: 800}`.
   Пресет `desktop` в этой сессии дал вьюпорт `0x0` — замеры были бы мусором.
   После установки размера всегда сверяйся: `innerWidth/innerHeight` не нулевые.
3. Логин: заполнить `textarea[name=db_connection_string]` и отправить форму.
4. Проверка страницы: `read_console_messages {onlyErrors:true}` должен быть пуст,
   `computer{action:'screenshot'}` — для глаз, `javascript_tool` — для чисел.

Минимальный набор замеров вёрстки (гонять на 1280×800 и на 375×812):

```js
const w = document.querySelector('#tableWrap');
JSON.stringify({
  bodyOverflow: document.body.scrollWidth - document.documentElement.clientWidth, // ДОЛЖЕН быть 0
  tableScrollable: w ? w.scrollWidth > w.clientWidth : null,   // таблица скроллится внутри себя
  rows: document.querySelectorAll('#accountsTable tbody tr').length
});
```

Критерии приёмки UI:
- Горизонтального скролла у `body` нет ни на одной ширине; широкая таблица
  скроллится **внутри** своего контейнера.
- Ничего не наезжает и не обрезается на 375px, 768px, 1280px, 1920px.
- Консоль без ошибок и без необработанных промисов; сеть без 4xx/5xx на обычном
  сценарии; `php_errors.log` без новых записей.
- Есть состояния: загрузка, пусто, ошибка. Пустая выдача — это отдельный
  проверяемый кейс, а не «ну там ничего нет».
- Тёмная тема не сломана (в проекте есть переключатель и `[data-bs-theme="dark"]`).
- Клавиатура: фокус видим, по Tab можно дойти до основных действий, у иконочных
  кнопок есть `aria-label`/`title`.
- Анимации не мешают: без дёрганий при обновлении таблицы и открытии панелей.

---

## Как писать и подключать модули (конвенции проекта)

Общее: **сначала ищи, где это уже есть**. Дубль логики в этом проекте уже приводил
к молчаливым багам (перезапись глобальных функций в `dashboard-stats.js`).

**PHP-бэкенд**
- Доступ к БД — только `Database::getInstance()`; `global $mysqli` в новом коде нет.
- Запросы — исключительно prepared statements; никакой конкатенации пользовательского
  ввода в SQL. Имена таблиц/колонок — через `TableResolver`/`ColumnMetadata`,
  не из запроса напрямую.
- Логика живёт в сервисе (`includes/*Service.php`), доступ к данным — в репозитории,
  сборка условий — в `FilterBuilder`. Точка входа (`*.php` в корне, `api/routes/*`)
  только валидирует вход и вызывает сервис.
- Логирование — `Logger`, а не `echo`/`var_dump`. Не логируй секреты, хост/логин БД,
  cookies, токены.

**API**
- Новый эндпоинт = маршрут в `api/routes/<resource>.php`; файл подключается в
  `api/index.php` и получает `$router` и `$tableName` из его scope. Auth и
  rate-limit уже навешаны глобальным middleware — не дублируй.
- Ответ — единый JSON-контракт (`success` + данные / `error`), коды HTTP по смыслу.
  401 для неавторизованных AJAX, а не редирект.

**JavaScript**
- Модуль — отдельный файл в `assets/js/modules/`, наружу торчит один объект
  `window.DashboardXxx = { init, ... }`, остальное внутри. Инициализация — из
  `dashboard-main.js`, а не в момент загрузки файла.
- Порядок подключения скриптов задан в шаблонах — новый модуль ставь в правильное
  место и проверь, что зависимости уже загружены.
- Логирование — `logger.debug/warn/error`, не `console.log`.
- DOM — через `domCache`, частые обработчики — через `debounce`/`batchDOM` из
  `performance.js`. Никаких дорогих операций в обработчике скролла.
- Ассеты подключаются с `?v=<?= ASSETS_VERSION ?>` — константа авто-бампится при
  деплое, руками её не трогай.

**Шаблоны**
- Разметка — в `templates/partials/...`, партиал самодостаточен: не полагайся на
  переменную, которая «должна быть» в scope. Нормализуй вход в начале файла
  (`$X = isset($X) && is_array($X) ? $X : [];`) — иначе Warning/фатал в проде.
- Любой вывод пользовательских данных экранируется (`e()` / `htmlspecialchars`).
- Изменил число колонок таблицы — проверь `colgroup`, `thead`, `rows.php`,
  `empty-state.php` и JS-рендер (`table-module.js`, `dashboard-refresh.js`):
  colspan/порядок колонок живут в пяти местах и легко расходятся.

---

## Комментарии и документация

Цель: новый программист открывает проект и понимает его без расспросов.

- Комментарий объясняет **почему**, а не пересказывает код. Лучший комментарий в
  этом репозитории — тот, что описывает грабли: «раньше было так, ломалось вот
  так, поэтому теперь так» (см. шапки `lint.yml`, `dashboard-stats.js`).
- У каждой публичной функции/класса — докблок: назначение, `@param`, `@return`,
  краевые случаи и опасности. У каждого файла-модуля — шапка: за что отвечает и
  чего в нём осознанно **нет**.
- Язык комментариев — русский, как и весь проект.
- Никаких «мёртвых» комментариев: закомментированный код удаляется, устаревший
  комментарий правится вместе с кодом. Комментарий, противоречащий коду, — баг.
- TODO обязан содержать причину и условие снятия, иначе он не нужен.
- Что обновлять при изменениях: `CHANGELOG.md` (что поменялось для пользователя),
  `DEVELOPER_GUIDE.md` (структура, модули, как добавить фичу), `USER_GUIDE.md`
  (если поменялся UI-сценарий), и раздел «Проверенные факты» ниже — если ты
  выяснил что-то новое про реальное поведение системы.
- Формат записи проверенного факта: **утверждение + чем и когда подтверждено**.
  Без «источника» факт в этот файл не попадает.

---

## Безопасность и данные

- Не портить данные важнее, чем показать результат. Сбой внешнего API/БД/таймаут —
  это «не проверено» (`errored`), а не бизнес-статус.
- Секреты (FTP, токены, пароли БД) — только в GitHub Secrets и в сессии. В коде,
  логах, коммитах и отчётах их быть не должно. `.claude/`, `*.log`, `users.json`,
  `.env` — в `.gitignore`, туда же уходят все временные артефакты.
- На проде и в чужом браузере — режим наблюдателя. Массовые операции, удаления и
  миграции на боевой БД — только по явному согласию владельца.
- Внутренние файлы (`tests/`, внутренние доки) не должны уезжать на публичный
  домен — см. список `exclude` в `deploy.yml`.

---

## Git, PR, деплой

- Коммиты и PR — по-русски, как и общение с владельцем. Формат сообщения:
  `тип(область): что сделано` (`fix(tests)`, `feat(dashboard)`, `perf(validation)`).
- Работаем в отдельной ветке, изменения едут через PR. Обязательный статус-чек —
  `lint`; без зелёного чека PR не сольётся.
- **Мёрдж в `main` = деплой в прод**, и мержить разрешено самому — условия и что
  делать после мёрджа см. в разделе «Мёрдж и деплой — делаю сам».
- В описании PR: что сделано, зачем, чем проверено (команды/замеры), что осталось.
- `origin` настроен на HTTPS, и его OAuth-токен **без scope `workflow`**: коммит,
  который трогает `.github/workflows/*`, по HTTPS отлетает с «refusing to allow an
  OAuth App to update workflow». Такие ветки пушим по SSH
  (`git push git@github.com:Ivan14044/accounts-dashboard.git <branch>`) — ключ
  настроен и работает. *(проверено 2026-08-08.)*

---

## Проверенные факты о проекте (подтверждены, а не «из доки»)

> Здесь — только то, что реально проверено. Дату/способ проверки указывай.

### Деплой и CI
- Автодеплой — workflow **Deploy to FTP** ([.github/workflows/deploy.yml](.github/workflows/deploy.yml)),
  триггер: **push в `main`** (и ручной). PR-уровневого деплоя нет. *(проверено по
  факту: мёрдж в main → запуск Deploy to FTP → success.)*
- Ветка `main` защищена и требует обязательный статус-чек **`lint`**
  ([.github/workflows/lint.yml](.github/workflows/lint.yml)). Без зелёного `lint`
  PR не сольётся. *(проверено: PR висел BLOCKED, пока чек не появился/прошёл.)*
  Отсюда важное следствие: **имя job обязано остаться `lint`**. Не превращай его в
  matrix — имена станут `lint (7.4)`/`lint (8.2)`, required-чек перестанет
  находиться и все PR встанут. Вторая версия PHP гоняется вторым `setup-php`
  внутри того же job.
- Гейт `lint` = `php -l` по всем PHP-файлам (PHP **7.3**) + все `tests/test_*.php`
  на PHP 7.3 и 8.2. *(проверено 2026-08-08 локально и в CI: `php -l` по всему
  репозиторию чист на 7.3, 7.4 и 8.2; все тесты зелёные на всех трёх — их
  десять на 2026-08-09, список растёт, поэтому число здесь не фиксируем.)*
  Нижняя граница именно 7.3, потому что прод не понимает синтаксис 7.4 —
  см. следующий пункт. Плюс в гейте поднимается сервис `mysql:8.0`, и на нём
  гоняются `tests/integration_*.php` — без него они молча скипались.
  *(добавлено 2026-08-08, первый прогон: 27 проверок на реальной БД.)*
- `ASSETS_VERSION` авто-бампится в деплое на короткий SHA коммита (cache-buster
  для CSS/JS). *(проверено grep по `*.php`.)*
- **Сборка бандлов есть с 2026-08-09.** Манифест — `includes/AssetBundles.php`
  (единственный источник порядка загрузки), сборщик — `tools/build_assets.php`,
  запускается шагом `Build asset bundles` в `deploy.yml` перед FTPS-загрузкой.
  Бандлов три: `core.css`, `dashboard.sync.js`, `dashboard.defer.js`. Файлы
  сгенерированные, в git не хранятся (`.gitignore`). Шаблон подключает бандл
  только если тот существует, иначе отдаёт исходники по одному — недоехавшая
  сборка не ломает прод. *(проверено на стенде: с бандлами 3 запроса, без них 43,
  набор глобалов в `window` в обоих режимах побайтово одинаков.)*
- Конкатенация JS в этом проекте **не бесплатна**: `logger`, `domCache`,
  `batchUpdater`, `Toast`, `DashboardMain` объявлены как top-level `const`/`class`.
  В отдельных `<script>` имя из файла N в файле N−1 просто не существует, и
  `typeof x !== 'undefined'` работает; в одном бандле те же объявления попадают в
  temporal dead zone на весь скрипт, и `typeof` уже **бросает**
  `Cannot access 'x' before initialization`, обрывая бандл целиком.
  *(проверено в браузере: первая версия бандла падала именно так; починено
  переносом `core/*` в начало списка, инвариант стережёт
  `tests/test_asset_bundles.php`.)*
- Из того же корня: голое имя `Toast` — это **класс** из `assets/js/toast.js`,
  а не экземпляр `window.Toast`. Лексическая привязка приоритетнее свойства
  `window`, статических `success/error/...` у класса нет. `modules/undo.js`
  из-за этого падал после успешной отмены и не обновлял таблицу.
  *(проверено в браузере и на бандле, и на отдельных файлах — `Toast ===
  window.Toast` там и там `false`; починено, стережёт
  `tests/test_toast_global_shadowing.php`.)*
- Прод отдаёт ассеты по **HTTP/2 с brotli**, `cache-control: max-age=14400`
  (4 часа, не год). `core-base.css`: 40 400 байт сырых → 8 836 по сети.
  *(проверено `curl -I --compressed` к панели 2026-08-09 — тогда трафик шёл
  через Cloudflare. С 2026-08-17 это уже не так, см. следующий пункт: brotli и
  HTTP/2 приходили от Cloudflare, а не от хостинга.)*
- **Cloudflare сейчас НЕ в цепочке, и HTTPS на проде сломан.** `dig +trace` от
  корня показывает делегирование `account-factory.site` на неймсерверы самого
  хостинга (`ns14/ns24/ns34.inhostedns.*`, `185.104.44.31` — соседняя подсеть с
  origin `185.104.45.9`), а не на Cloudflare. Регистратор сбросил NS, когда
  домен ушёл в `clientHold` после истечения 2026-08-16. Трафик идёт напрямую на
  origin, а тот на **любое** имя домена (`panel.`, `www.`, apex, `dashboard.`)
  отдаёт заглушку `CN=default-ssl.default-host.net` → обычный `curl` падает с
  «no alternative certificate subject name matches». Зона в Cloudflare при этом
  жива (edge-сертификат `CN=account-factory.site` от Google Trust Services
  действует до 2026-11-03), но домен на неё не делегирован.
  *(проверено 2026-08-17. Осторожно: `dig +short NS` через системный резолвер
  ещё отдавал закэшированные NS Cloudflare — верить надо `+trace` от корня.)*
  Переезд на новый домен описан в `docs/DOMAIN_MIGRATION.md`.
- **Origin не доверяет `X-Forwarded-Proto` / `CF-Visitor`.** Запрос к
  `185.104.45.9` по HTTP с этими заголовками и без них даёт одинаковый ответ:
  `Location: http://…` и cookie без флага `secure`. То есть `$_SERVER['HTTPS']`
  выставляется только реальным TLS до origin. **Следствие: SSL-режим Cloudflare
  «Flexible» для панели непригоден** — редирект `index.php → login.php` уедет на
  `http://`, а cookie сессии потеряет `secure`. Нужен Full; `Full (strict)` —
  только после установки собственного сертификата на origin (иначе 526).
  Текущая зона стоит в Full: запрос через прокси-адреса Cloudflare отдал 302 с
  `Location: https://…` и cookie `secure`, а не 526. *(проверено 2026-08-17.)*
- **Боевой адрес панели — `https://panel.accdash.com`** (переезд выполнен
  2026-08-17). Домен куплен у Cloudflare Registrar, зона `accdash.com` на их
  NS, запись `panel A 185.104.45.9` проксируется, SSL-режим **Full (strict)**,
  Universal SSL активен и покрывает `*.accdash.com`. На origin стоит Cloudflare
  Origin Certificate (SAN `*.accdash.com`, `accdash.com`, до 2041-08-13), ключ
  генерировался локально и Cloudflare не передавался. Let's Encrypt на этом
  хостинге при включённом проксировании выпустить нельзя — adm.tools отказывает
  прямым текстом. На хостинге домен подключён
  **псевдонимом** к сайту `panel.account-factory.site` — каталог остался
  `/home/if592995/panel.account-factory.site/www/`, поэтому FTP-деплой и
  `server_dir: ./` не менялись. *(проверено: `/login.php` → 200, `/index.php` →
  302 на `https://panel.accdash.com/login.php` с cookie `secure`,
  `deployed-version.txt` → 200, `tools/`,`includes/`,`sql/`,`*.md` → 403,
  сертификат `CN=accdash.com` от Google Trust Services, verify code 0.)*
  Старый `panel.account-factory.site` пока тоже отвечает, но HTTPS на нём битый.
  Детали и грабли переезда — `docs/DOMAIN_MIGRATION.md`.
- **Cloudflare подмешивает свой beacon, и наша CSP его блокирует.** Edge
  вставляет `static.cloudflareinsights.com/beacon.min.js` в HTML проксируемой
  зоны, а `script-src` его не пускает → ошибка в консоли на каждой странице.
  Скрипт при этом не загружается, данные наружу не уходят.
  **Выключателя в дашборде нет — не ищи заново.** Проверено 2026-08-17 на
  `accdash.com`: зоны нет в аккаунт-уровневом списке Web Analytics (перебраны
  все 35 сайтов на 4 страницах), RUM в зоне показан выключенным, Speed Brain и
  остальные оптимизации выключены, а токен в beacon не равен `site_tag` записей
  из списка (у `welmaria.com` site_tag `778168921b…`, токен beacon `5624bd4f…`),
  поэтому `/web-analytics/edit/<токен>` редиректит на список. Варианты решения
  и их цена — в `docs/DOMAIN_MIGRATION.md`.
  Важно для диагностики: beacon подмешивается только
  в ответы на «браузероподобные» запросы — `curl -A "Mozilla/5.0"` его не
  увидит, нужны ещё `Accept: text/html,…` и `Sec-Fetch-*`. *(проверено
  2026-08-17: без этих заголовков grep по HTML пуст, с ними — beacon есть.)*
- **PHP на проде старее 7.4.** Точный номер по-прежнему не виден (`X-Powered-By`
  скрыт, заголовки отдаёт только nginx хостинга + `x-ray`), но верхняя
  граница доказана: `tools/migrations/apply_indexes_safe.php` отдавал по HTTP
  `Parse error: syntax error, unexpected '=>' (T_DOUBLE_ARROW) ... on line 138`,
  а на строке 138 стояла стрелочная функция `fn($n) => ...`, доступная только с
  7.4. *(проверено запросом к проду 2026-08-08; воспроизведено локально:
  `php:7.3-cli` даёт ровно такую же ошибку, `php:7.4-cli` — нет.)*
  Следствие: пишем код, совместимый с 7.3, и линтуем снизу на 7.3.
- Прод показывал текст Parse error прямо в браузере вместе с абсолютным путём
  `/home/if592995/panel.account-factory.site/www/...` — то есть `display_errors`
  в php.ini включён, а `ini_set` из `config.php` не помогает: ошибка парсера
  случается до выполнения кода. Погашено на уровне `.htaccess`
  (`php_flag display_errors Off`), логирование оставлено включённым.
  *(проверено 2026-08-08 до и после правки.)*
- Служебные каталоги закрыты в `.htaccess`: `includes/`, `tools/`, `sql/`,
  `tests/`, `docs/`, `openspec/`, плюс `*.md` и `*.sql` → 403. До правки
  `sql/create_all_tables.sql` отдавал схему БД целиком, а `includes/*.php` —
  пустой HTTP 200 (файл выполнялся). *(проверено запросами к проду до и после.)*

### Локальное окружение
- PHP на машине владельца **нет** — только Docker (`php:7.4-cli`, `php:8.2-cli`,
  `mysql:8.0`). *(проверено: `php -v` → command not found.)*
- `mysql:8` (тег latest-мажор) стартует и сразу падает с
  `--default-authentication-plugin` — этой опции больше нет. Использовать
  `mysql:8.0` без неё. *(проверено 2026-08-08.)*
- Полный стенд поднимается и логинится — рецепт в разделе «Локальный стенд»,
  проверен целиком: дашборд отдал HTTP 200, схема создалась автомиграцией,
  страница отрисовалась в браузере без ошибок консоли.

### Проверка аккаунтов на валидность (check.fb.tools)
Поток: фронт [dashboard-validate.js](assets/js/modules/dashboard-validate.js) режет
выбор на партии и шлёт на наш `POST /api/accounts/validate/check`
([api/routes/accounts.php](api/routes/accounts.php)) → сервер
[AccountValidationService.php](includes/AccountValidationService.php) дробит на
суб-батчи и параллельно (`curl_multi`) обращается к check.fb.tools → ответ
маппится в valid / invalid / **errored (не проверено)**.

Реальный контракт API *(проверено живыми curl-запросами)*:
- `POST https://check.fb.tools/api/check/account`, тело
  `{"inputData":[...],"checkFriends":false,"userLang":"ru"}`.
- Ответ: `data[].account`, `data[].uid`, `data[].status.name` (`valid`|`invalid`),
  плюс `data[].status.message` и сводка `info:{valid,invalid,errors,noExist}`.
- Размер партии: API принимает **минимум 5000 ID за запрос** (HTTP 200). Тайминги:
  3000 → ~4.7с, 5000 → ~8с. «3000» — не жёсткий лимит. Текущий
  `FB_TOOLS_BATCH_SIZE=100` сильно занижен ⇒ лишние десятки запросов ⇒ риск
  rate-limit. Кандидат на увеличение (с запасом по `FB_TOOLS_TIMEOUT`).
- Комментарий `CONCURRENCY=1 — иначе nginx 429` относится к **нашему** серверу,
  а не к check.fb.tools.

КРИТИЧНО по корректности: сбой/таймаут/частичный ответ внешнего API **нельзя**
трактовать как `invalid` — иначе валидные аккаунты портятся статусом. Такие
случаи идут в `errored` («не проверено»). См. фикс в `checkItems` /
`checkFbIdsBulk` / `parseHandleResult`.

### Производительность дашборда
- **Четырёх колонок фильтров нет в эталонной схеме, но они есть на проде:**
  `status_marketplace`, `currency`, `geo`, `status_rk`. В
  `DatabaseSchemaManager::getRequiredSchema()` их нет, весь читающий код ходит
  к ним через `columnExists()`. Следствие, которое стоило много времени:
  **стенд, поднятый эталоном, физически не воспроизводит прод** — тяжёлые
  запросы по этим колонкам там просто не выполняются.
  Замер на 182 021 строке: без этих колонок страница 1,91 с холодная / 0,05 с
  прогретая; с ними — 2,72 с / 0,29–0,35 с. Хочешь мерить прод локально —
  сначала добери эти колонки в стенд. *(проверено 2026-08-10.)*
  Список продублирован в `Database::OPTIONAL_INDEX_COLUMNS`; `ensureIndexes()`
  молча пропускает индекс, если колонки в этой БД нет.
- **Медленно на проде не «прогретая страница», а пересчёт агрегатов.** Ключ
  файлового кэша содержит отпечаток `MAX(updated_at)`, поэтому ЛЮБАЯ правка
  любой строки инвалидирует все агрегаты сразу и для всех фильтров. На живом
  дашборде, где кто-то постоянно что-то меняет, пользователь почти всегда
  попадает именно в пересчёт. Владелец видел 1,6–1,7 с — это он и есть.
  *(проверено 2026-08-10: замер пересчёта на стенде дал 1,52–1,60 с.)*
- Разбор пересчёта по запросам (slow log с `long_query_time=0`, 182 021 строка,
  до оптимизации): UNION уникальных значений фильтров 0,894 с; `GROUP BY status`
  со счётчиком «за 24 часа» 0,632 с; общий `COUNT(*)` со счётчиками 0,298 с;
  счётчики пустых фильтров 0,223 с; стартовая точка спарклайна 0,199 с. Итого
  2,25 с из 2,32 с страницы — то есть страница целиком упирается в БД, PHP
  почти ничего не стоит. *(проверено 2026-08-10.)*
- Что дало ускорение: `getEmptyFilterCounts()` обёрнут в `StatsCache` (был
  единственным тяжёлым агрегатом без кэша) и добавлены покрывающие индексы.
  Ключевой из них — `idx_deleted_status_dates (deleted_at, status, updated_at,
  created_at)`: двух колонок мало, потому что за датами для счётчика «за 24
  часа» приходится лезть в строки. 0,788 с → 0,042 с. Он же закрывает оба
  запроса спарклайна — отдельный `(deleted_at, created_at)` проверен и оказался
  лишним. Итог: пересчёт 2,2–2,3 с → 0,68–0,77 с, прогретая 0,29–0,35 с →
  0,05–0,06 с. *(проверено 2026-08-10 на стенде.)*
- **Флаг «индексы проверены» обязан включать имя БД.** Вход — строка
  подключения, одна установка обслуживает много баз. Пока имени базы в ключе
  флага не было, первая открытая база ставила флаг и глушила `ensureIndexes()`
  для всех остальных: они оставались вообще без управляемых индексов.
  *(воспроизведено 2026-08-10: вторая база получила 6 индексов от самой схемы и
  ни одного из 11 `MANAGED_INDEXES`; после сброса флага создались все.)*

### Таблица аккаунтов
- Колонок в `colgroup` ровно `count($ALL_COLUMNS) + 3` (checkbox, favorite,
  actions), и `colspan` строки «Ничего не найдено» обязан совпадать. Абсолютное
  число зависит от колонок в БД, поэтому проверяй инвариант, а не константу.
  *(проверено на стенде 2026-08-08 после добавления `id_soc_account` в эталон:
  47 колонок данных → 50 `<col>` и `colspan="50"`; до неё было 46 → 49.)*
- Известные грабли: `templates/partials/table/empty-state.php` вызывал `count()`
  без аргумента — на 7.4 Warning в лог, на 8.x фатал при **пустой** выдаче.
  Починено, покрыто `tests/test_empty_state_colspan.php`.
- Структура `accounts` задаётся в **трёх** расходящихся местах, а индексы — в
  четвёртом: эталон `DatabaseSchemaManager::getRequiredSchema()` (де-факто главный,
  вызывается из `auth.php` при каждом коннекте), inline `CREATE TABLE` в
  `config.php` (только если таблицы нет вообще), `sql/*.sql` (руками) и список
  `Database::MANAGED_INDEXES`. Правишь индексы — сверься с эталоном; это стережёт
  `tests/test_schema_index_columns.php`.
- Эталон аддитивен и **безопасен для прода**: добирает недостающие колонки
  `ALTER ... ADD COLUMN`, но никогда не меняет тип существующей колонки и ничего
  не удаляет. *(проверено на mysql:8: существующий `VARCHAR(64)` уцелел.)*
- Починенные грабли: индекс `idx_id_soc_account` ссылался на `id_soc_account`,
  которой не было в эталоне → `[ERROR] Failed to create index` при каждом заходе.
  Вопреки прежней записи «на работу не влияет» — влияло: `MassTransferService`
  и `admin_duplicates.php` читают эту колонку голым SQL, без `columnExists()`,
  поэтому на БД, созданной эталоном, массовый перенос падал с исключением, а
  страница дубликатов молча показывала пустоту. Колонка добавлена в эталон
  (`VARCHAR(255)`), покрыто `tests/test_schema_index_columns.php`.
  *(проверено 2026-08-08 на стенде и на mysql:8.)*
- Колонка считается опциональной, только если **все** её читатели идут через
  `columnExists()`/`columnsList`. Проверяй грепом, а не на глаз.
- **На проде есть ещё две колонки вне эталона: `passkey` и `phone_removed`**
  (идут сразу после `two_fa`). `passkey` хранит JSON вида
  `{"rp_id": "accounts.meta.com", "credential_id": "…"}` — семантика «есть/нет».
  *(проверено 2026-08-23 чтением живой страницы прода: заголовки
  `<th data-column>` и сортировка по колонке.)*
- **Контракт `phone_removed` (со слов владельца, 2026-08-23):** `'1'` — номер
  снят нашим софтом, пусто (NULL) — не снят. `'1'` пишется **только в момент
  реального снятия**, то есть третьего состояния «не проверяли» у колонки нет и
  пустота — это полноценный ответ «не снят», а не отсутствие данных.
  Наблюдения совпадают: значения `'0'` в таблице не встречалось.
  Фильтр дашборда (`FilterBuilder::addPresenceFilter`) строит именно это:
  «удалён» = `IS NOT NULL AND <> ''`, «не удалён» = `IS NULL OR = ''`.
  Проверка на `<> ''` — страховка на случай пустой строки вместо NULL, на
  контракт `'1'`/NULL она не влияет.
  Колонка заведена недавно: на 2026-08-23 заполнена у ОДНОЙ записи на всю базу
  (`?sort=phone_removed&dir=desc` давал `'1'` только в первой строке). Фильтр
  рабочий, но выборка будет расти по мере работы софта — не пугайся пустоты.
- **Обновление таблицы не должно зависеть от прихода кадра.** Симптом, который
  раньше был записан здесь как «крестик на chip не обновляет таблицу»: URL
  правится, чекбокс снимается, а таблица и чипы остаются от прошлого фильтра.
  Прежний диагноз («гонка поколений» refresh) оказался **неверным** — гонки там
  нет: новый refresh отменяет предыдущий синхронно, поэтому живой запрос всегда
  один, и последний ответ всегда применялся бы последним. Настоящая причина —
  вся DOM-фаза `refreshDashboardData` (таблица, чипы, снятие прелоадера) лежала
  внутри `requestAnimationFrame`, а кадр браузер обязан выдать далеко не всегда.
  Доказательство (стенд, счётчик кадров самоподдерживающимся rAF-циклом):
  после клика по крестику `raf=1` (ни одного кадра за 5 с) — URL `?page=1`,
  строк 1, чип на месте; после форсирования кадра `raf=6` — строк 5, чипов нет.
  Обновление применялось ровно в момент прихода кадра.
  **Ловушка для проверяющего:** встроенная браузерная панель отдаёт
  `document.visibilityState === 'hidden'` и сама кадров не рисует — кадр
  выдаётся только на `computer{action:'screenshot'}`. Любой замер UI, зависящий
  от rAF/анимаций, без форсирования кадра врёт. Именно на этом и родился
  прежний неверный диагноз. Починено 2026-08-23: фаза 2 применяется синхронно,
  в rAF остался только layout-догон (ширины, плотность, виртуализация) — его
  пропуск без кадра безвреден. Инвариант стережёт
  `tests/test_filter_chip_refresh.php`.
- **У крестиков `.filter-chip-remove` не должно быть inline `onclick`.** В
  `templates/partials/dashboard/filters.php` их было 22 при уже работающем
  делегированном обработчике в `filters-modern.js` — на один клик приходилось
  два вызова `removeFilterChip()` и два запроса `refresh.php`, из которых первый
  тут же отменялся `abort()` второго. Чипы, отрисованные из JS
  (`renderActiveFiltersFromUrl`), inline `onclick` не имеют и всегда жили на
  делегировании, то есть серверная разметка расходилась с клиентской. Снято
  2026-08-23, замер после фикса: один вызов на клик вместо двух.

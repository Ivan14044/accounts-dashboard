# 📝 CHANGELOG - Dashboard Project

Все значимые изменения в проекте документированы здесь.

---

## [Не выпущено]

### Добавлено
- **При импорте снова видно предпросмотр файла и прогресс.** Перед загрузкой
  показываются первые строки CSV и общее их число, во время импорта — полоса
  прогресса и кнопка «Отменить импорт». Раньше эта разметка лежала в файле,
  который не попадал на страницу, поэтому ничего из перечисленного не появлялось —
  молча, без ошибок. Заодно исправлена оценка прогресса: полоса сразу прыгала
  почти на конец независимо от размера файла, теперь растёт соразмерно.
- **В форме импорта появился выбор, что делать с уже существующими аккаунтами:**
  «Пропустить» (по умолчанию), «Обновить» или «Показать ошибку». Раньше форма
  всегда молча отправляла «Пропустить» — остальные два режима поддерживались
  сервером, но были недоступны из интерфейса. Перед запуском «Обновить»
  спрашивается подтверждение: он перезаписывает данные, а импорт нельзя отменить.

### Для разработчиков
- Удалены ещё шесть неподключённых партиалов модалок (`bulk-edit-modal`,
  `cell-modal`, `custom-card-modal`, `delete-confirm-modal`, `status-modal`,
  `transfer-modal`) — все 52 их `id` уже есть в `dashboard.php`, то есть это были
  чистые дубликаты. В каталоге остались только три реально подключаемых партиала.
  Появился тест `tests/test_templates_reachable.php`: он требует, чтобы каждый
  файл в `templates/` был достижим по цепочке include хотя бы от одной страницы.
  **Найдено по пути, не трогал:** `templates/partials/dashboard/toolbar.php` тоже
  не подключён ниоткуда и расходится с живым `partials/table/toolbar.php` на
  65 строк; занесён в список известных долгов внутри теста.
- Удалён `templates/partials/dashboard/modals/add-account-modal.php` — копия
  формы импорта, не подключённая ни одним шаблоном. Именно из-за неё выбор режима
  дубликатов выглядел реализованным, хотя пользователю был недоступен: правки
  уезжали в копию, а рендерилась другая разметка. Восстанавливается из истории git.
  **Найдено по пути, не трогал:** в том же каталоге ещё шесть партиалов не
  подключены нигде (`bulk-edit-modal`, `cell-modal`, `custom-card-modal`,
  `delete-confirm-modal`, `status-modal`, `transfer-modal`), а в рабочей форме
  импорта нет контейнеров прогресс-бара и предпросмотра CSV, к которым обращается
  `dashboard-upload.js` — они существовали только в удалённой копии.

### Изменено
- **CSS и JS дашборда собираются в бандлы.** Страница тянула 43 своих файла
  (35 JS + 8 CSS), теперь три: `assets/build/core.css`,
  `dashboard.sync.js`, `dashboard.defer.js`. По брутли — как реально отдаёт прод —
  166 373 → 134 323 байта (−19,3 %). Список файлов и их порядок переехали в единый
  манифест `includes/AssetBundles.php`; собираются бандлы шагом деплоя, а не в
  рантайме. Если бандла на сервере нет, шаблон отдаёт исходники по одному —
  сломанная сборка не роняет страницу.

- **Дашборд стал заметно быстрее на больших таблицах.** На стенде с прод-формой
  данных (182 021 запись, 72 статуса): страница после любой правки данных —
  2,2–2,3 с → **0,68–0,77 с**, страница целиком из кэша — 0,29–0,35 с →
  **0,05–0,06 с**. Что сделано: счётчики пустых фильтров перестали пересчитываться
  на каждый заход (единственный агрегат, который не кэшировался), и добавлены пять
  покрывающих индексов под запросы дашборда. Первый заход после обновления будет
  разовым долгим — база строит индексы.

### Исправлено
- **Импорт в режиме «Обновить» затирал колонки, которых нет в файле.** Скачанный
  шаблон содержит в заголовке все колонки; если заполнить в нём только `login` и
  `status` и загрузить с режимом «Обновить», у существующего аккаунта обнулялись
  пароль, почта, имя, `id_soc_account`, заметки и всё остальное — а отчёт говорил
  «Обновлено: 1» без единого предупреждения. Восстановить было нечем: импорт не
  пишет в журнал отмены. Теперь пустая ячейка означает «не трогать это поле».
  Очистить значение импортом больше нельзя — это делается в таблице.
  *(Из интерфейса режим «Обновить» сейчас недоступен: форма всегда отправляет
  «Пропустить». Затирание было достижимо только прямым запросом к
  `import_accounts.php`.)*
- **Импорт молча терял все строки после случайной кавычки в CSV.** Если в любом
  поле файла встречалась одиночная кавычка (в имени, заметке, extra_info),
  импорт на боевом сервере обрывал чтение и загружал только строки до неё —
  и рапортовал успех. Файл из 10 строк с кавычкой в третьей давал 3 записи без
  единого предупреждения. Проявлялось только на проде: на новых версиях PHP тот
  же файл читался целиком.
- **Клик по карточке статуса на давно открытой вкладке давал белый экран.**
  Если страница провисела до перевыпуска сессии, проверка защитного токена
  роняла запрос целиком: сервер отдавал 500 и пустое тело. Теперь страница
  открывается нормально и говорит, что нужно её обновить.
- **Ошибки в запросах к API отдавались как «сбой сервера».** Кривой JSON,
  просроченный токен, неверные параметры — всё это возвращало 500. Теперь
  ошибка ввода — 400, отказ по токену — 403, а 500 остаётся только за
  настоящими сбоями сервера.
- **Сообщения об ошибках массовой смены статуса нигде не показывались.** Их
  записывали в сессию, но не выводили: операция могла упасть, а пользователь
  видел обычную страницу и считал, что всё применилось.
- **База, отличная от первой открытой, работала вообще без индексов.** Вход в
  панель — это строка подключения, то есть одна установка обслуживает сколько
  угодно баз. Отметка «индексы уже проверены» не учитывала имя базы, поэтому
  первая же открытая база глушила создание индексов для всех остальных: они
  оставались с полным сканом на каждом запросе.
- **Строка таблицы «прыгала» при наведении мыши.** Панель кнопок
  (редактировать / копировать / показать пароль) вставлялась прямо в поток
  ячейки, а таблица считает ширину колонок по содержимому. Из-за этого каждое
  наведение меняло ширину колонки и переставляло всю таблицу целиком: при
  проводке мыши вдоль строки сдвигалось 35–42 ячейки, максимум на 73,7 px, и
  каждый переход между ячейками стоил 2,6–12,6 мс пересчёта раскладки при
  бюджете кадра 16,7 мс. Теперь панель висит над правым краем ячейки вне потока:
  при том же замере — 0 сдвинутых ячеек, 0 px. Панель больше не заезжает и на
  соседнюю колонку (узкие колонки перекрывались на ~12 px).
- **Ячейка навсегда «толстела» после каждой правки.** Редакторы обычного поля и
  пароля сохраняли содержимое ячейки, чтобы вернуть его после сохранения или
  отмены, — и захватывали в этот снимок ещё и панель кнопок. При восстановлении
  её копия вклеивалась в ячейку намертво: ячейка `last_name` росла со 108,9 до
  174,3 px, `password` — со 102,8 до 213,5 px, и в ней оставался второй комплект
  кнопок. Эффект накапливался с каждой отредактированной ячейкой.
- **Отмена действия (undo) не показывала результат и не обновляла таблицу.**
  После успешного отката модуль падал на `Toast.success(...)`: голое имя `Toast` —
  это класс из `assets/js/toast.js`, а не экземпляр `window.Toast` с методами.
  Ошибка улетала в `catch`, где падала повторно, и до обновления данных дело не
  доходило. В БД откат при этом происходил — то есть пользователь видел старые
  данные и думал, что отмена не сработала.
- **Свайп по карточке статистики на телефоне молча ничего не делал.** Причин было
  две, каждая сама по себе фатальная: жесты вешались на класс `touch-card`,
  которого нет ни в одной разметке (слушатели не создавались вообще), а решение
  «что делать со свайпом» принималось по атрибуту `data-card-type`, который
  сервер не рендерит. Теперь свайп влево по карточке статуса применяет фильтр по
  этому статусу без перезагрузки страницы, а по карточке «Всего» — показывает
  подсказку. Кастомные карточки, появляющиеся уже после загрузки, работают без
  переинициализации.

---

## [2.3.0] - 2026-08-08

Полный аудит проекта ([docs/AUDIT_2026-08-08.md](docs/AUDIT_2026-08-08.md)) и
работа по его итогам. Каждая правка подтверждена тестом и живой проверкой.

### Исправлено
- **Выгрузка истории изменений портила значения с обратными слэшами.**
  `admin_logs.php` писал CSV дефолтным `fputcsv` (escape='\\'), из-за чего
  cookies, token и пути не переживали обратное чтение. Запись CSV теперь одна на
  проект — `includes/Csv.php`; формат выгрузок не менялся.
- **Ошибка в запросе отдавалась как «500, произошла ошибка сервера».** Кривой
  JSON или отсутствующий CSRF теперь дают понятный `400` и JSON, а не
  HTML-страницу; страница ошибки при 4xx говорит «Некорректный запрос», а не
  отправляет искать поломку в логах. В логе больше нет бессмысленного
  «Invalid JSON format: No error» — пишется настоящая причина.
- **С телефона нельзя было пользоваться пагинацией:** список страниц вылезал за
  экран и обрезался. Теперь переносится по строкам.
- **Кнопка «Перейти» обрезала свою подпись** на всех размерах экрана.
- **Пустая таблица роняла страницу** на PHP 8 (`count()` без аргумента).

### Изменено
- Дашборд запрашивает кастомные карточки один раз за загрузку вместо трёх.
- Модули подписываются на обновление таблицы через
  `DashboardRefresh.onAfterRefresh()` вместо переопределения глобальной функции —
  сбой одного модуля больше не ломает остальные.

### Безопасность
- Закрыты по HTTP служебные каталоги `includes/`, `tools/`, `sql/` и файлы
  `*.sql`. До этого `sql/create_all_tables.sql` отдавал схему БД целиком.
- Отключён показ текстов PHP-ошибок посетителю: прод показывал Parse error
  вместе с абсолютным путём на сервере. Логирование сохранено.

### Для разработчиков
- Обнаружено и зафиксировано: **PHP на проде старее 7.4**. Гейт `lint` теперь
  линтует на 7.3 (нижняя граница) и гоняет тесты на 7.3 и 8.2 — раньше
  синтаксис 7.4 проходил проверку и падал на живом сайте.
- Тестов в CI-гейте стало 8 вместо 5: добавлено покрытие импорта и записи CSV,
  выбора HTTP-кодов, пустой выдачи таблицы. Диагностический скрипт, который
  печатал «FAIL», но всегда завершался успехом, убран из гейта.
- Удалён мёртвый код, который при этом уезжал на прод:
  `assets/js/features/custom-cards.js` (дубль логики карточек, не подключался ни
  одним шаблоном), `deploy.php` (FTP-загрузчик, дублировал GitHub Actions и
  требовал несуществующих переменных окружения) и `build_assets.php` (сборщик
  бандла, который ни одна страница не подключает). Все три восстанавливаются из
  истории git.

---

## [2.2.0] - 2026-07-04

### ⚡ Плавность панели (замеры на стенде: MySQL 10k строк, headless Chrome)

#### Изменено
- **«Тонкая» разметка строк таблицы** — кнопки копирования/редактирования больше
  не рендерятся в каждой ячейке (их даёт одна плавающая hover-панель,
  `assets/js/modules/cell-actions.js`), а полные значения длинных полей
  (cookies, token и т.п.) не кладутся в HTML целиком — подгружаются по
  требованию (`field_value.php`) или берутся из данных строки в JS.
  Замер: строка 30 КБ → 16 КБ; HTML страницы 1.7 МБ → 0.95 МБ (50 строк),
  6.4 МБ → 3.4 МБ (200 строк); стоимость шага виртуализации при скролле
  (рендер+layout) 41 мс → 15 мс (медиана) — скролл при 200/стр перестал дёргаться.
  Разметка синхронно изменена во всех трёх рендерерах: `templates/partials/table/rows.php`,
  `table-module.js`, `dashboard.js`.
- **Debounce 250мс на чекбоксы статусов и быстрые фильтры** — серия быстрых
  кликов даёт один запрос вместо запроса на каждый клик (замер: 4 клика → 1 запрос).
- **Bootstrap и Font Awesome теперь локальные** (`assets/vendor/`) — убраны
  блокирующие рендер CDN-запросы (~0.5 с) и зависимость от cdn.jsdelivr/cdnjs.
- Автообновление (30 с) не дёргает сервер в фоновой вкладке (visibilitychange);
  `history.php`/`log.php` больше не ломают кэш браузера `?v=time()`.

#### Исправлено
- **Просмотр полного значения длинного поля по клику не работал** (обработчик
  `[data-full]` жил в неинициализируемом классе `Dashboard`) — просмотр
  реализован заново в `cell-actions.js` и работает, включая подгрузку значения.

### ↩️ Отмена последнего действия (undo)

#### Добавлено
- **Кнопка «Отменить последнее»** в тулбаре дашборда — показывает последнее отменяемое
  действие текущего пользователя и откатывает его с подтверждением
- **Журнал действий `user_actions`** — каждая мутация (смена статуса, правка поля,
  массовые операции, удаление в корзину, массовый перенос) фиксируется как одно
  логическое действие; строки `account_history` связываются через `action_id`
- **`undo.php`** — API: `GET` последнее отменяемое действие, `POST` откат
- **`includes/UndoService.php`** — логика отката: оптимистичная проверка
  (строки, изменённые после действия, не трогаются), чанки по 500,
  чувствительные поля (`[СКРЫТО]`) пропускаются, повторный откат блокируется атомарно,
  сам откат логируется как действие типа `undo` (не отменяем)
- **Тосты с кнопкой действия** (`toast.js` — опция `action`), предложение отмены
  в тосте после операций
- Тесты: `tests/test_undo_plan.php` (без БД), `tests/integration_undo_mysql.php`
  (полный путь на реальном MySQL, запуск через Docker — см. заголовок файла)

#### Исправлено
- **AuditLogger::logBulkChange читал старые значения всегда из `accounts`** —
  для других таблиц (`?table=xxx`) история писалась с неверными old_value.
  Теперь таблица передаётся явно
- **Аудит массового переноса не писался вовсе** — `logBulkChange` вызывался после
  UPDATE внутри транзакции, старые значения совпадали с новыми и все строки
  отбрасывались как «без изменений». Лог перенесён до UPDATE
- **Аудит «удалить все по фильтру» терял строки** — ID собирались после удаления
  эвристикой «deleted_at за последнюю минуту LIMIT 100» (максимум 100 строк,
  возможен захват чужих удалений). Теперь ID собираются до удаления, без лимита

---

## [2.1.0] - 2025-01-27

### 🐛 Исправления безопасности

#### Исправлено
- **XSS уязвимости** - заменены все `innerHTML` на безопасные методы (`textContent`, `escapeHtml()`)
  - `quick-search.js`, `dashboard.js`, `saved-filters.js`, `toast.js`, `table-module.js`, `favorites.js`
- **CSRF защита** - добавлена валидация CSRF токенов во всех API endpoints
  - `api_custom_card.php`, `api_user_settings.php`, `api_favorites.php`, `status_update.php`, `delete.php`, `update_field.php`, `api_saved_filters.php`, `api_register_status.php`, `delete_permanent.php`, `empty_trash.php`, `duplicate.php`
- **SQL injection** - заменены небезопасные `SHOW INDEX` и `SHOW TABLES` на prepared statements с `INFORMATION_SCHEMA`
- **JSON input validation** - добавлена функция `read_json_input()` с ограничением размера (1MB) для всех API endpoints
- **Session timeout** - исправлена логика обновления времени сессии (обновляется только раз в 5 минут, а не на каждый запрос)

#### Добавлено
- Функция `read_json_input()` в `includes/Utils.php` для безопасного чтения JSON
- Валидация CSRF токенов через `Validator::validateCsrfToken()` во всех изменяющих запросах

---

### 🔧 Исправления inline-редактирования

#### Исправлено
- **Неправильная типизация данных** - добавлена нормализация значений по типам колонок БД
  - Числовые поля (int, float) корректно преобразуются перед сохранением
  - NULL значения обрабатываются правильно
  - Строковые поля сохраняются как строки
- **Отсутствие валидации на клиенте** - добавлена проверка типов перед отправкой на сервер
  - Числовые поля проверяются на корректность
  - Показываются ошибки валидации пользователю
- **Плохая обработка ошибок** - улучшена обработка ошибок при сохранении
  - Восстановление исходного значения при ошибке
  - Показ понятных сообщений об ошибках через toast
- **Проблемы с виртуализацией** - добавлена блокировка скролла во время редактирования
  - Предотвращает потерю контекста при скролле
  - Улучшает UX при редактировании

#### Добавлено
- Метод `normalizeValueByColumnType()` в `AccountsRepository.php` для корректной типизации
- Атрибуты `data-field-type` в HTML для клиентской валидации
- Блокировка скролла в `table-module.js` во время редактирования

---

### 🗑️ Очистка данных

#### Исправлено
- **Орфанные записи** - добавлена очистка связанных данных при удалении аккаунтов
  - Удаление из `account_favorites` при hard delete
  - Удаление из `account_history` при hard delete
- **Копирование deleted_at** - исправлено дублирование аккаунтов (поле `deleted_at` не копируется)

#### Добавлено
- Метод `cleanupRelatedData()` в `AccountsRepository.php` для очистки связанных записей

---

### 📊 Улучшения производительности

#### Добавлено
- Индекс `idx_deleted_at` на колонке `deleted_at` для ускорения запросов с soft delete

---

### 🛡️ Улучшения безопасности логирования

#### Исправлено
- **Утечка чувствительных данных** - добавлено маскирование паролей, токенов и других чувствительных полей в логах
  - Пароли заменяются на `[СКРЫТО]`
  - Токены и cookies маскируются
- **SQL injection в логировании** - исправлено использование prepared statements в `AccountsService::restoreAccounts`

#### Добавлено
- Метод `isSensitiveField()` в `AuditLogger.php` для определения чувствительных полей
- Автоматическое маскирование чувствительных данных перед логированием

---

### 🔄 Улучшения импорта

#### Исправлено
- **CSV parsing** - заменен `explode("\n")` на `fgetcsv()` для корректной обработки многострочных полей
- **Memory exhaustion** - реализована потоковая обработка больших файлов вместо загрузки всего файла в память

---

### 🧹 Рефакторинг и очистка

#### Удалено
- Временные статусные файлы (.txt): `APPLY_NOW.txt`, `DEPLOYMENT.txt`, `FINAL_SUMMARY.txt`, `QUICK_DEPLOY.txt`, `READY_TO_DEPLOY.txt`, `START_HERE.txt`
- Устаревшие статусные .md файлы: `CHECKBOX_ALIGNMENT_FIXED.md`, `SOCIAL_URL_EDIT_FIXED.md`, `ТАБЛИЦА_ПЕРЕПИСАНА.md`, `ФИНАЛЬНЫЙ_СТАТУС.md`, `ИНСТРУКЦИЯ_ЗАПУСКА.md`, `OPTIMIZATION_COMPLETE.md`, `OPTIMIZATION_GUIDE.md`, `OPTIMIZATION_SUMMARY.md`, `QUICK_START_OPTIMIZATION.md`, `README_OPTIMIZATION.md`, `PERFORMANCE_CHECKLIST.md`, `Z-INDEX_HIERARCHY.md`
- Старые build файлы с хешами (18 файлов)
- Неиспользуемые CSS файлы: `dashboard.css`, `dashboard-main.css`, `dashboard-pro.css`, `FORCE_CACHE_CLEAR.css`
- Дублирующиеся скрипты: `apply_indexes.php`, `register_missing_status.php`, `apply_optimizations.bat`, `apply_optimizations.sh`

#### Обновлено
- `README.md` - создан единый обновленный файл с актуальной информацией
- `CHANGELOG.md` - добавлена информация о всех последних исправлениях

---

## [2.0.0] - 2025-11-12

### ⚡ Производительность

#### Улучшено
- **Оптимизация запросов статистики:** 8 запросов → 2-4 запроса (4-5x быстрее)
- **Загрузка дашборда:** 800ms → 150-200ms
- **Добавлены индексы БД:** idx_email_status, idx_two_fa, idx_token (2-5x ускорение фильтров)
- **Кэширование статистики:** TTL 5 минут

#### Добавлено
- Класс `Config` с константами производительности
- Методы `cache()` и `getCached()` в Database класс

---

### 🛡️ Безопасность

#### Добавлено
- **Rate Limiting** для всех API endpoints
  - Login: 5 req/min (защита от brute-force)
  - Export: 10 req/min
  - API: 100 req/min
- **Система уровневого логирования** (Logger класс)
  - Автоматическая фильтрация чувствительных данных
  - Debug отключается в production
- **HTTP 429 Too Many Requests** при превышении лимитов

#### Улучшено
- Удалено избыточное логирование из API endpoints
- Размер логов уменьшился с 50MB/день до ~10MB/день

---

### 🎨 Дизайн и UX

#### Добавлено
- **Единая дизайн-система** (design-system.css)
  - Полная цветовая палитра
  - Spacing scale (4-96px)
  - Typography scale
  - Shadow scale
  - Dark theme support
- **Glass Morphism эффект** для фильтров
  - Полупрозрачный белый фон
  - Backdrop blur эффект
  - Premium внешний вид
- **Toast Notifications** вместо alert()
  - 4 типа: success, error, warning, info
  - Автоскрытие через 3 сек
  - Неблокирующие уведомления
- **Современные фильтры**
  - Визуальные chips для активных фильтров
  - iOS-style toggle switches
  - Улучшенное поле поиска
  - Каждый статус отдельным chip

#### Изменено
- **Применение фильтров только по кнопке**
  - Пользователь выбирает все фильтры
  - Нажимает "Применить" один раз
  - 3x быстрее для множественного выбора
- **Индикация несохранённых изменений**
  - Пульсирующая кнопка "Применить"
  - Предупреждение при уходе со страницы
- Дополнительные фильтры всегда видимые

---

### 📝 Код качество

#### Добавлено
- Класс `Config` - все константы централизованы
- Класс `Logger` - уровневое логирование
- Класс `RateLimiter` - ограничение запросов
- `RateLimitMiddleware` - helper функция

#### Улучшено
- **Рефакторинг export.php** - удалено ~100 строк дублирующего кода
- **Единообразное логирование** во всех API endpoints
- **Дублирование кода:** 15% → <5%
- **Magic numbers:** много → 0

---

### ⌨️ Keyboard Shortcuts

#### Добавлено
- `Ctrl + F` - Фокус на поиск
- `Ctrl + Enter` - Применить фильтры
- `Escape` - Очистить поиск
- `Tab` - Навигация по элементам

---

### 📚 Документация

#### Добавлено
- `openspec/project.md` - Контекст проекта
- `openspec/AGENTS.md` - Инструкции для AI
- `openspec/README.md` - Руководство по OpenSpec
- `openspec/QUICKSTART.md` - Быстрый старт
- `DESIGN_SYSTEM.md` - Руководство по дизайн-системе
- `DESIGN_QUICK_REFERENCE.md` - Шпаргалка
- `UNIFIED_DESIGN_REPORT.md` - Отчёт об унификации
- `FILTERS_FINAL_REPORT.md` - Отчёт о фильтрах
- `GLASS_MORPHISM_FILTERS.md` - Glass эффект
- `FILTERS_APPLY_BUTTON_FEATURE.md` - Кнопка применения
- `FILTERS_USAGE_GUIDE.md` - Руководство пользователя
- `PROJECT_IMPROVEMENTS_COMPLETE.md` - Полный отчёт
- `CHANGELOG.md` - Этот файл

---

### 🔧 Технические изменения

#### Добавлено
- `includes/Logger.php` (187 строк)
- `includes/Config.php` (213 строк)
- `includes/RateLimiter.php` (208 строк)
- `includes/RateLimitMiddleware.php` (114 строк)
- `assets/css/design-system.css` (400+ строк)
- `assets/css/components-unified.css` (550+ строк)
- `assets/css/filters-modern.css` (500+ строк)
- `assets/js/toast.js` (182 строки)
- `assets/js/filters-modern.js` (270 строк)

#### Изменено
- `includes/Database.php` - добавлены методы кэширования
- `includes/AccountsService.php` - оптимизирована статистика
- `export.php` - рефакторинг под AccountsService
- `delete.php`, `mass_transfer.php`, `update_field.php`, `bulk_update_field.php` - Logger
- `login.php`, `status_update.php` - Rate Limiting
- `templates/dashboard.php` - новый дизайн фильтров
- `assets/css/toast.css` - использует design-system переменные
- `assets/js/dashboard.js` - отключено автоприменение поиска

---

### 🗑️ Устаревшее (можно удалить)

#### Рекомендуется удалить:
- `assets/css/dashboard.css` - заменён на design-system.css
- `assets/css/dashboard-main.css` - заменён на components-unified.css
- `assets/css/dashboard-pro.css` - возможно не используется

**Примечание:** Перед удалением проверьте что они нигде не подключаются.

---

## [1.0.0] - 2025-11-10

### Добавлено
- Первая версия Dashboard
- Управление аккаунтами
- Фильтрация и сортировка
- Экспорт в CSV/TXT
- Массовые операции
- Inline редактирование

---

## 🔮 Планируется (Roadmap)

### v2.1.0 (опционально)
- [ ] CSV/JSON импорт аккаунтов
- [ ] История изменений (Audit Log)
- [ ] Сохранённые фильтры
- [ ] Теги для аккаунтов

### v2.2.0 (опционально)
- [ ] Расширенная аналитика с графиками
- [ ] REST API с документацией
- [ ] Webhook уведомления
- [ ] Миграция users.json в БД

### v3.0.0 (долгосрочно)
- [ ] Real-time обновления (WebSockets)
- [ ] Collaborative editing
- [ ] Advanced permissions system
- [ ] Multi-tenancy support

---

**Текущая версия:** 2.0.0  
**Статус:** ✅ Production Ready  
**Следующий релиз:** TBD








